{
    "Servers": {
        "1": {
            "Name":      "Local (compose)",
            "Group":     "Servers",
            "Host":      "db",
            "Port":      5432,
            "MaintenanceDB": "postgres",
            "Username":  "app",
            "SSLMode":   "prefer",
            "Comment":   "Auto-registered by docker-compose. Password is asked on first connect — use the value of DB_PASSWORD from your .env."
        }
    }
}
